import React from 'react';
import { Typography } from '@mui/material';

const Contact = () => (
  <div>
    <Typography variant="h4">Contact Page</Typography>
    <Typography>You can reach me at your@email.com.</Typography>
  </div>
);

export default Contact;