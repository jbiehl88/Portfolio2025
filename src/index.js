import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // assuming App.js exists in the src folder

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);